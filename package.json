{
  "name": "termo-english",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test:test": "yarn workspace ui test",
    "test:storybook": "yarn workspace ui storybook",
    "fix": "yarn fix:lint && yarn fix:format",
    "fix:lint": "yarn eslint --fix",
    "fix:format": "yarn prettier . --fix -w",
    "test:lint": "yarn eslint",
    "test:format": "yarn prettier . -c",
    "build:ui": "yarn workspace ui build",
    "clean:ui": "yarn workspace ui clean"
  },
  "engines": {
    "node": "14",
    "yarn": "3",
    "npm": "please-use-yarn"
  },
  "workspaces": [
    "ui",
    "eslint-config",
    "app"
  ],
  "devDependencies": {
    "@storybook/builder-webpack5": "next",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
